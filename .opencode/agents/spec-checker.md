---
description: Verifies the acceptance criteria of a spec — marks checkboxes, fixes code issues, and reports results.
agent: spec-checker
mode: subagent
---

Verify the acceptance criteria of the spec `$ARGUMENTS`.

Find the spec file in `specs/` (the user may have written the number like `06`, the slug like `crear-publicacion-modal`, or the full name `06-crear-publicacion-modal`). Read it, locate the acceptance criteria section, verify each criterion, fix any code issues you find, mark the checkboxes in the spec file, and report a summary at the end.
