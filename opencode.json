{
  "$schema": "https://opencode.ai/config.json",
  "lsp": true,
  "command": {
    "spec": {
      "description": "Crea especificaciones de pantallas y funcionalidades",
      "template": "Carga y sigue la skill spec en .agents/skills/spec/SKILL.md.\n\nFeature: $ARGUMENTS"
    }
  },
  "references": {
    "docs": {
      "path": "../07-DB-Schema",
      "description": "Schema debase de datos, tablas, columnas y referencias a crear, esto no está implementado en la base de datos real, solo es un esquema de referencia para crear la base de datos y las tablas."
    }
  },
  "permission": {
    "external_directory": {
      "/tmp/**": "allow"
    },
    "bash": "allow"
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@playwright/mcp@latest"
      ],
      "enabled": true
    }
  }
}